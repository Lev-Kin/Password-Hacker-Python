# Given list of strings
# list_of_strings = input().split()

# Convert each element to an integer and increase it by 1
result = [int(value) + 1 for value in list_of_strings]

# Print the resulting list
print(result)
